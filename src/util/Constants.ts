export type Settings = {
    Language: string
};

/**
 * The ranges for numerical values, as well as their increments.
 */
export const SettingsRanges = {
    Language: [ "English" ]
};

/**
 * The default settings that Iodite takes.
 */
export const DefaultSettings: Settings = {
    Language: "English"
};